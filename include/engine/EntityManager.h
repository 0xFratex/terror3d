#pragma once

#include <string>
#include <memory>

namespace ExperimentRedbear {

// Entity manager for entity-component-system architecture

class EntityManager {
public:
    EntityManager() = default;
    ~EntityManager() = default;
};

} // namespace ExperimentRedbear
